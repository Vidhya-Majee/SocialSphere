<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-900 via-fuchsia-900 to-cyan-900">
    <div class="w-full max-w-lg mx-auto p-1 rounded-2xl shadow-2xl">
        <div class="p-10 rounded-2xl bg-gray-800 bg-opacity-90 border-4 border-fuchsia-800 flex flex-col shadow-2xl">
            <div class="w-full flex justify-end mb-4">
                <a href="/logout" class="bg-red-500 rounded-md px-4 py-2 text-sm font-bold text-white">Logout</a>
            </div>
            <h1 class="text-3xl font-extrabold mb-6 text-white drop-shadow-lg text-center tracking-tight uppercase">Edit Your Post <span class="text-yellow-400">🚀</span></h1>
            <form action="/update/<%= post._id %>" method="post" class="flex flex-col gap-5 items-center">
                <textarea class="p-4 outline-none w-full min-h-[120px] bg-gray-900 bg-opacity-70 border-2 border-fuchsia-700 rounded-xl text-white text-lg font-bold placeholder-fuchsia-400 focus:ring-2 focus:ring-cyan-400 shadow-md resize-none" name="content" placeholder="What's on your mind"><%= post.content %></textarea>
                <button type="submit" class="bg-gradient-to-r from-fuchsia-700 via-cyan-700 to-yellow-500 text-white font-extrabold py-3 px-8 rounded-xl shadow-xl text-lg transition duration-200 transform hover:scale-105 w-40">Update Post</button>
            </form>
        </div>
    </div>
</body>
</html>